import React from "react";
import { BeatLoader } from "react-spinners";

const Loading = () => (
  <div className="sweet-loading">
    <BeatLoader color={"#209dee"} />
  </div>
);

export default Loading;
